
  # Disability Care Website

  This is a code bundle for Disability Care Website. The original project is available at https://www.figma.com/design/KNNE8M3MrriyQACofEKaua/Disability-Care-Website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
  ## Pelase add the following code to next.config.mjs
  const nextConfig = {
    output: 'export',
    trailingSlash: true,    
};

export default nextConfig;# mydisabilitycare
